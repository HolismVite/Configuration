import { Enum } from '@Form'

const ConfigType = () => {
    return <Enum
        column='ConfigTypeId'
        placeholder='Config Type'
        entityType='ConfigType'
    />
}

export default ConfigType